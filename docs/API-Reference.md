# API Reference

## Endpoints
POST /api/address/create — Generate new z-address
GET /api/balance/<addr> — Check address balance
POST /api/transaction/send — Send ZEC transaction

## Authentication
API key via Authorization header.
